config_terminator
copyq.conf
warp_config.json

{{ if ne .chezmoi.os "darwin" }}
Google Drive
Applications/myocr.app
Applications/myqr.app
{{ end }}

# oh-my-zsh writes these at runtime; never track them as managed files.
.oh-my-zsh/cache
.oh-my-zsh/log
.zcompdump*

# Handy's runtime directory can contain API keys, transcripts, and recordings.
# Its curated settings are applied by run_onchange_after_apply-handy-settings.
Library/Application Support/com.pais.handy
