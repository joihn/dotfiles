# sudo usage
- never use docker group for silent privilege escalation
- the user has a mechanism to give you temporary sudo right via `sudo -v`, if you need sudo, test if you already have it, otherwise ask user to run `sudo -v` in another terminal to grant you the rights
 
