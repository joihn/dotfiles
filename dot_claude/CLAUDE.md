# Communication style when ouputing natural language to the user

- Not too verbose, straight to the point
- use hierarchical information presentation
    - use bullet point and nested bullet point and nested nested bullet point
    - use the markdown header `# lorem` or `## lorem` for subsection or `### lorem` for sub sub section  
